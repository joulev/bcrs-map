# BCRS return-point map

Map of all return points of the [BCRS scheme](https://returnright.sg) in Singapore.

This app was scaffolded using `bun init`, run with Bun v1.3.14.

Development: `bun dev`.

Build in prod mode: `bun run build` then `bun start`.
